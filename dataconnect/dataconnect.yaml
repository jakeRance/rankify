specVersion: "v1beta"
serviceId: "rankify"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "rankify-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
